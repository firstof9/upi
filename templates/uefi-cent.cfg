default=0
timeout=5
splashimage=(nd)/splash.xpm.gz
hiddenmenu

title CentOS %version%
        root (nd)
        kernel /boot/centos/%version%/vmlinuz ksdevice=%omac% load_ramdisk=1 network ks=http://%myip%/%mac%.cfg
        initrd /boot/centos/%version%/initrd.img
		
