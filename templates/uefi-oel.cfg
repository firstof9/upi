default=0
timeout=5
splashimage=(nd)/splash.xpm.gz
hiddenmenu

title Oracle Enterprise Linux %version%
        root (nd)
        kernel /boot/oel/%version%/vmlinuz ksdevice=%omac% load_ramdisk=1 network ks=http://%myip%/%mac%.cfg
        initrd /boot/centos/%version%/initrd.img
